Arima 
1. It first applies differencing (d) to tranform raw prices into stationary returns, effectively stripping away the upward or downard drift to reveal the underlying signal. 
2. The model then use autoregression (p) to capture the momentum from previous days and moving average(q) to smooth out random "noise". 
3. I think the model does not require validation test therefore increasign the training set to a ratio so that the test set is the same as other baseline model 
4. Wrote a function checkstationary by using the adfuller (if value is less than 0.05 than it is stationary else it is not) to see if return_price is stationary, regardless the model should be able to handle if the stationary is non-stationary by setting the parameter "stationary=False"
5. The Augmented Dickey-Fuller(ADF) test the null hypothesis (Ho) that a unit root is present (non-stationary) against the alternative hypothesis (Ha) that the series is stationary. A lower p-value indicates rejection of Ho. Stationary means data has contant mean, variance, and autocorrelation over time. (Ho) is Null hypothesis, 

- AIC score to see if the model 

LSTM 


BI-LSTM 
